# Public deploy URL. No secrets here — it is inlined into the client bundle at
# build time, which is why it must exist before `npm run build`.
# Change this if you later add a custom domain, then rebuild and redeploy.
NEXT_PUBLIC_SITE_URL=https://portfolio-6b6ee.web.app
